%macro println 1
    LOAD R7, %1
    MOV R6, 1
    INT 0
%endmacro

db info, "While loop(Counter to 500)"

mov r1, 50

; r0 - in-loop counter
; r1 - max number
label while:
    add r0, 1 ;add 1 to R0
    cmp r0, r1
    jl while

; now r0 is 50